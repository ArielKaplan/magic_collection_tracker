# Dependencies
node_modules/

# Build output
dist/
out/
release/
src/renderer/dist/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.claude/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Env / local config
.env
.env.local

# SQLite runtime files (the database lives in %APPDATA%, not the repo)
*.db
*.db-shm
*.db-wal

# electron-builder cache
electron-builder.cache/

# package-lock isn't needed for an Electron app distributed as an .exe;
# keep this commented if you DO want reproducible installs across machines
# package-lock.json
